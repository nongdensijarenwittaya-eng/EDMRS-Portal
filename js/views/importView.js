/* ==========================================================================
   EDMRS - Import View Controller (Removed)
   ========================================================================== */

const importView = {
  render() {
    return `<div class="card"><p class="text-muted">แท็บนี้ถูกยกเลิกการใช้งานแล้ว</p></div>`;
  },
  initEvents() {}
};

window.importView = importView;
